import type { Flow } from "../types/flow";
export const mockFlows: Flow[] = [{ id: "flow_1", projectId: "project_1", name: "Alta de cliente", environment: "sandbox", status: "pending", createdAt: new Date().toISOString(), updatedAt: new Date().toISOString() }];

