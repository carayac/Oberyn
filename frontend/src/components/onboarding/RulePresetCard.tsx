import { Card } from "../ui/Card";
export function RulePresetCard({ title = "Preset de regla" }: { title?: string }) { return <Card><p className="text-sm font-medium text-slate-950">{title}</p></Card>; }

